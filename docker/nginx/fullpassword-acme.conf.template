# Managed by DevFlow Full Password proxy adapter. Independent virtual host.
server {
    listen 80;
    server_name __DEVFLOW_DOMAIN__;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        add_header Retry-After 60 always;
        return 503;
    }
}
